let base = 5;
let altura = 7;

let areaTriangulo = (base, altura) => {
  return base * altura / 2
}


console.log(`El área de un triángulo de Base ${base} y altura ${altura} es:
  ${areaTriangulo(base, altura)}`);
